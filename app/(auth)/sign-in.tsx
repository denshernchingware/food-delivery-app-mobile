import { Redirect } from "expo-router";
import { Text } from "react-native"

const SignIn = () => {
    // const isSingnedIn = true;
    // if(!isSingnedIn)return <Redirect href={'/sign-up'}/>
    return (
        <Text>this is sign in page</Text>
    )
}
export default SignIn